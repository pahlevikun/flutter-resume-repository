import 'package:flutter/widgets.dart';

class Education {
  String title;
  String field;
  String year;
  String institution;
  String description;

  Education(
      {@required this.title,
      @required this.field,
      @required this.year,
      @required this.institution,
      @required this.description});
}

import 'package:flutter/widgets.dart';

class Volunteer {
  String imagePath;
  String title;
  String duration;
  String name;

  Volunteer(
      {@required this.imagePath,
      @required this.title,
      @required this.duration,
      @required this.name});
}
